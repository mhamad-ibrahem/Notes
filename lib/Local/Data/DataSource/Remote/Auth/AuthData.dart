// import '../../../../../Global/Core/Class/Crud.dart';
// import '../../../../Core/Constant/ApiLinks.dart';
import '../../../Model/Auth/CheckEmialModel.dart';
import '../../../Model/Auth/SignInModel.dart';
import '../../../Model/Auth/SignUpModel.dart';

class AuthData {
  // Crud crud;
  // AuthData(this.crud);

  signIn(SignInModel model) async {
    // var response = await crud.postData(AppLinks.serverLink, model.toJson(), {});
    // return response.fold((l) => l, (r) => r);
  }

  signUp(SignUpModel model) async {
    // var response = await crud.postData(AppLinks.serverLink, model.toJson(), {});
    // return response.fold((l) => l, (r) => r);
  }
  checkEmail(CheckEmialModel model) async {
    // var response = await crud.postData(AppLinks.serverLink, model.toJson(), {});
    // return response.fold((l) => l, (r) => r);
  }
}
